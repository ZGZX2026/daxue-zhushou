/* 数据库原理(MySQL) 题库数据 */
UH.reg('mysql', {
  intro: '数据库原理研究数据的组织、存储、查询与管理，以关系模型和SQL为核心。本课程结合MySQL讲解数据库设计（ER模型、三大范式）、SQL语言、约束、索引、事务与并发控制。',
  tips: [
    'SQL子句执行顺序心里要有：FROM/JOIN→WHERE→GROUP BY→HAVING→SELECT→ORDER BY→LIMIT。',
    'WHERE在分组前过滤行（不能用聚合函数），HAVING在分组后过滤组（可以用聚合函数）。',
    '连接分清：INNER取交集，LEFT以左表为准补NULL；连接条件放ON与放WHERE在左连接中结果不同。',
    '范式抓“主键与依赖”：1NF原子性、2NF消除部分依赖、3NF消除传递依赖。',
    '事务记ACID；InnoDB支持事务和外键，MyISAM不支持；索引是空间换时间。'
  ],
  facts: [
    {t:'数据库DB', d:'长期存储在计算机内、有组织、可共享的大量数据集合，具有较小冗余、较高数据独立性和易扩展性。', tag:'数据库基础'},
    {t:'数据库管理系统DBMS', d:'位于用户与操作系统之间、科学组织和管理数据的系统软件，负责数据定义、操纵、运行控制与维护，如MySQL。', tag:'数据库基础'},
    {t:'三级模式', d:'外模式（用户视图）、概念模式（全局逻辑结构）、内模式（物理存储结构），两级映像保证逻辑独立性与物理独立性。', tag:'数据库基础'},
    {t:'数据模型三要素', d:'数据结构（对象与联系）、数据操作（增删改查等）、完整性约束；常见模型有层次、网状、关系、面向对象模型。', tag:'数据库基础'},
    {t:'关系型数据库', d:'用二维表（关系）组织数据，表由行（元组）和列（属性）组成，用SQL操作，代表产品有MySQL、Oracle、SQL Server。', tag:'数据库基础'},
    {t:'关系（表）', d:'一个关系对应一张二维表：元组（行/记录）、属性（列/字段）、域（取值范围）、候选键、主键、外键。', tag:'关系模型'},
    {t:'主键', d:'能唯一标识表中每行且非空、最小的属性（组）；一张表只能有一个主键，主键值不能重复也不能为NULL。', tag:'关系模型'},
    {t:'外键', d:'一张表中引用另一张表主键（或唯一键）的列，用于建立两表联系并保证参照完整性：外键值必须在父表存在或为NULL。', tag:'关系模型'},
    {t:'ER模型', d:'实体-联系模型用于概念设计：矩形表示实体、椭圆表示属性、菱形表示联系；联系有1:1、1:n、m:n三种基数。', tag:'关系模型'},
    {t:'完整性约束', d:'实体完整性（主键非空唯一）、参照完整性（外键要么为空要么引用存在的主键）、用户定义完整性（NOT NULL/CHECK/默认值等）。', tag:'关系模型'},
    {t:'SQL分类', d:'DDL数据定义（CREATE/ALTER/DROP）、DML数据操纵（INSERT/UPDATE/DELETE）、DQL查询（SELECT）、DCL控制（GRANT/REVOKE）、TCL事务（COMMIT/ROLLBACK）。', tag:'SQL基础'},
    {t:'SELECT基本结构', d:'SELECT 列 FROM 表 WHERE 行条件 ORDER BY 排序列 LIMIT 行数；*表示全部列，可用AS给列起别名。', tag:'SQL查询'},
    {t:'WHERE条件', d:'WHERE过滤满足条件的行，可用比较符、BETWEEN…AND、IN集合、LIKE模糊匹配（%任意多字符、_单个字符）、IS NULL判空。', tag:'SQL查询'},
    {t:'ORDER BY排序', d:'ORDER BY 列 ASC升序（默认）/DESC降序，可多列排序；分页用LIMIT 偏移量,行数 或 LIMIT 行数 OFFSET 偏移量。', tag:'SQL查询'},
    {t:'DISTINCT', d:'SELECT DISTINCT 列 对结果按该列去重，只保留不同取值；NULL视为相同的一个值。', tag:'SQL查询'},
    {t:'聚合函数', d:'COUNT计数、SUM求和、AVG平均、MAX/MIN求最值；除COUNT(*)外聚合函数自动忽略NULL。', tag:'SQL高级查询'},
    {t:'GROUP BY分组', d:'按某列把相同值的行分为一组，配合聚合函数统计每组；SELECT中的非聚合列必须出现在GROUP BY中。', tag:'SQL高级查询'},
    {t:'HAVING', d:'对GROUP BY分组后的结果按聚合条件过滤（如HAVING COUNT(*)>2）；WHERE不能用聚合函数，过滤组要用HAVING。', tag:'SQL高级查询'},
    {t:'内连接', d:'INNER JOIN ... ON 条件只返回两表连接条件匹配成功的行，是最常用的连接，等价于在WHERE中写等值连接条件。', tag:'SQL高级查询'},
    {t:'左外连接', d:'LEFT JOIN以左表全部行为基准，右表无匹配时补NULL；RIGHT JOIN反之；FULL OUTER JOIN保留两边全部行（MySQL不直接支持）。', tag:'SQL高级查询'},
    {t:'子查询', d:'嵌套在另一条SQL中的SELECT，可位于WHERE（配合IN/比较符/EXISTS）、FROM（派生表）、SELECT列（标量子查询）中。', tag:'SQL高级查询'},
    {t:'IN与EXISTS', d:'IN判断值是否在子查询结果集合中；EXISTS判断相关子查询是否返回行，数据量与索引不同时两者各有性能优势。', tag:'SQL高级查询'},
    {t:'UNION', d:'UNION合并多个SELECT结果并自动去重（要求列数、类型兼容），UNION ALL不去重、速度更快。', tag:'SQL高级查询'},
    {t:'CREATE TABLE', d:'建表时定义列名、数据类型（INT、DECIMAL、VARCHAR、DATE、DATETIME、TEXT等）及列级/表级约束。', tag:'数据定义与约束'},
    {t:'NOT NULL与DEFAULT', d:'NOT NULL强制列必须有值（实体/业务完整性），DEFAULT为插入时未指定值的列提供默认值。', tag:'数据定义与约束'},
    {t:'UNIQUE与AUTO_INCREMENT', d:'UNIQUE保证列值不重复（允许NULL）；AUTO_INCREMENT使整数列在插入时自动生成递增编号，常配合主键使用。', tag:'数据定义与约束'},
    {t:'ALTER与DROP', d:'ALTER TABLE增删改列或约束（ADD/MODIFY/CHANGE/DROP COLUMN）；DROP TABLE删除表结构与数据且通常不可回滚。', tag:'数据定义与约束'},
    {t:'INSERT/UPDATE/DELETE', d:'INSERT INTO 表(列) VALUES(值)插入；UPDATE 表 SET 列=值 WHERE 条件；DELETE FROM 表 WHERE 条件；后两者忘写WHERE会改动全表。', tag:'数据定义与约束'},
    {t:'视图VIEW', d:'由查询定义的虚表，CREATE VIEW名AS SELECT…，不实际存数据（普通视图），可简化查询、限制列访问、提供逻辑独立性。', tag:'数据定义与约束'},
    {t:'索引原理', d:'索引是为加速查找而建立的数据结构（InnoDB默认B+树），相当于书的目录，以额外存储空间和写入开销换取查询提速。', tag:'索引'},
    {t:'索引类型', d:'主键索引（聚簇，数据行按其组织）、唯一索引、普通索引、联合索引（遵守最左前缀原则）、全文索引。', tag:'索引'},
    {t:'何时建索引', d:'频繁出现在WHERE、JOIN ON、ORDER BY中的列适合建索引；数据量小、频繁更新、区分度低的列不宜盲目建索引。', tag:'索引'},
    {t:'最左前缀原则', d:'联合索引(a,b,c)可用于a、(a,b)、(a,b,c)的查询条件，不能跳过最左列直接用b或(c,b)。', tag:'索引'},
    {t:'事务', d:'事务是由一条或多条SQL组成的不可分割的逻辑执行单位，要么全部成功提交，要么全部失败回滚。', tag:'事务与并发'},
    {t:'ACID', d:'原子性（不可分割）、一致性（提交前后数据满足完整性约束）、隔离性（并发事务互不干扰）、持久性（提交后永久保存）。', tag:'事务与并发'},
    {t:'并发问题', d:'脏读（读到未提交数据）、不可重复读（两次读同一行结果不同）、幻读（两次范围查询行数不同）。', tag:'事务与并发'},
    {t:'隔离级别', d:'读未提交、读已提交、可重复读（MySQL InnoDB默认）、串行化；级别越高隔离越强、并发越低。', tag:'事务与并发'},
    {t:'锁机制', d:'共享锁（读锁S）可并发读，排他锁（写锁X）独占；InnoDB行锁锁索引记录，锁不命中索引会升级为表锁。', tag:'事务与并发'},
    {t:'第一范式1NF', d:'每个属性都必须是不可再分的原子值，表中不能有多值字段或重复组（如“电话1,电话2”塞一列不满足1NF）。', tag:'数据库设计'},
    {t:'第二范式2NF', d:'在1NF基础上消除非主属性对主键的部分函数依赖（针对复合主键）：每个非主属性要依赖整个主键而不是其中一部分。', tag:'数据库设计'},
    {t:'第三范式3NF', d:'在2NF基础上消除非主属性对主键的传递依赖：非主属性不能决定另一个非主属性（如学号→院系→院系电话应拆表）。', tag:'数据库设计'},
    {t:'函数依赖与候选键', d:'X→Y表示X确定则Y唯一确定；能函数决定全部属性的最小属性组为候选键，选其一为主键。', tag:'数据库设计'},
    {t:'反范式', d:'为提升查询性能，有意引入冗余（合并表、增加汇总列）降低连接次数，以空间和写入一致性维护换取读性能。', tag:'数据库设计'},
    {t:'存储过程', d:'预编译并存放在数据库中的一组SQL语句集合，用CALL调用，可带参数、流程控制，减少网络往返但移植性较差。', tag:'事务与并发'},
    {t:'MySQL常用函数', d:'字符串CONCAT/SUBSTRING、数值ROUND/FLOOR、日期NOW()/DATE_FORMAT、条件CASE WHEN、IFNULL将NULL替换为默认值。', tag:'SQL基础'}
  ],
  core: [
    {q:'数据库中能唯一标识表中每一行且不能为空的列（组）称为（　）。', o:['外键','主键','索引','视图'], a:1, d:1, tag:'关系模型', e:'主键唯一且非空，体现实体完整性。'},
    {q:'外键的主要作用是（　）。', o:['加快查询','保证参照完整性','加密数据','自动排序'], a:1, d:2, tag:'关系模型', e:'外键值必须引用父表中存在的主键，维护表间一致性。'},
    {q:'查询学生表中年龄大于18的学生姓名，正确SQL是（　）。', o:['SELECT 姓名 FROM 学生','SELECT 姓名 FROM 学生 WHERE 年龄>18','SELECT * WHERE 年龄','FROM 学生 SELECT 姓名'], a:1, d:1, tag:'SQL查询', e:'基本结构SELECT列FROM表WHERE条件。'},
    {q:'WHERE与HAVING的区别是（　）。', o:['没有区别','WHERE过滤行，HAVING过滤分组且可用聚合函数','HAVING先执行','WHERE可以用COUNT()'], a:1, d:2, tag:'SQL高级查询', e:'WHERE分组前过滤行且不能用聚合函数，HAVING分组后过滤组。'},
    {q:'统计每个部门的员工人数应使用（　）。', o:['ORDER BY','GROUP BY COUNT(*)','DISTINCT','LIMIT'], a:1, d:2, tag:'SQL高级查询', e:'GROUP BY部门，再用COUNT(*)统计每组行数。'},
    {q:'左外连接LEFT JOIN的结果（　）。', o:['只保留两表匹配行','保留左表全部行，右表无匹配补NULL','只保留右表行','两表行数相加'], a:1, d:2, tag:'SQL高级查询', e:'左连接以左表为基准，右表不匹配处为NULL。'},
    {q:'模糊查找姓名中包含“张”的记录，条件应写（　）。', o:["姓名 = '张'","姓名 LIKE '_张'","姓名 LIKE '%张%'","姓名 IN ('张')"], a:2, d:2, tag:'SQL查询', e:'%匹配任意多个字符，%张%表示包含张。'},
    {q:'MySQL InnoDB默认的事务隔离级别是（　）。', o:['读未提交','读已提交','可重复读','串行化'], a:2, d:2, tag:'事务与并发', e:'InnoDB默认可重复读（REPEATABLE READ）。'},
    {q:'事务ACID特性不包括（　）。', o:['原子性','并发性','隔离性','持久性'], a:1, d:2, tag:'事务与并发', e:'ACID是原子性、一致性、隔离性、持久性，没有“并发性”。'},
    {q:'联合索引(a,b,c)在下列查询中能被利用的是（　）。', o:['WHERE b=1','WHERE a=1 AND b=2','WHERE c=3','WHERE b=1 AND c=3'], a:1, d:3, tag:'索引', e:'最左前缀原则，必须从a开始连续使用。'},
    {q:'关系模式属于第二范式2NF应消除（　）。', o:['部分函数依赖','传递函数依赖','所有函数依赖','多值依赖'], a:0, d:3, tag:'数据库设计', e:'2NF在1NF基础上消除非主属性对主键的部分依赖；3NF才消除传递依赖。'},
    {q:'学号→院系、院系→院系电话，存在的问题是（　）。', o:['部分依赖','传递依赖，不满足3NF','满足BCNF','没有问题'], a:1, d:2, tag:'数据库设计', e:'院系电话经院系传递依赖于学号，应拆表以满足3NF。'},
    {q:'给学生表新增一列phone(VARCHAR(11))应使用（　）。', o:['DROP TABLE','ALTER TABLE 学生 ADD phone VARCHAR(11)','DELETE FROM','CREATE INDEX'], a:1, d:2, tag:'数据定义与约束', e:'ALTER TABLE … ADD COLUMN用于增加列。'},
    {q:'UPDATE语句忘记写WHERE子句会导致（　）。', o:['只更新一行','报错中止','全表所有行被更新','什么都不发生'], a:2, d:1, tag:'数据定义与约束', e:'UPDATE/DELETE缺WHERE作用于全表，是经典事故。'},
    {q:'UNION与UNION ALL的区别是（　）。', o:['没有区别','UNION去重，UNION ALL保留重复且更快','UNION ALL只能合并两表','UNION更快'], a:1, d:2, tag:'SQL高级查询', e:'UNION去重排序有额外开销，确认无重复时用UNION ALL。'},
    {q:'下列关于索引说法正确的是（　）。', o:['索引越多越好','索引会占用存储并降低写入速度，但能加速查询','索引只能在主键上建','索引会改变数据内容'], a:1, d:2, tag:'索引', e:'索引以空间和写性能换读性能，需权衡。'},
    {q:'下列SQL关键字中属于DDL的有（　）。', o:['CREATE','ALTER','DROP','INSERT'], a:[0,1,2], d:2, tag:'SQL基础', e:'CREATE/ALTER/DROP是数据定义语言；INSERT是DML。', m:1},
    {q:'关于脏读、不可重复读、幻读，下列说法正确的有（　）。', o:['脏读是读到了别的事务未提交的数据','不可重复读针对同一行两次读结果不同','幻读针对范围查询行数变化','串行化级别可避免这些问题'], a:[0,1,2,3], d:3, tag:'事务与并发', e:'四条均为并发问题及隔离级别的正确描述。', m:1},
    {q:'判断：一张关系表可以有多个主键。（　）', o:['正确','错误'], a:1, d:1, tag:'关系模型', e:'一张表只能有一个主键（可以是复合主键），但可以有多个UNIQUE列。', type:'judge'},
    {q:'判断：聚合函数COUNT(*)会自动忽略列中的NULL。（　）', o:['正确','错误'], a:1, d:2, tag:'SQL高级查询', e:'COUNT(*)统计行数不忽略NULL；只有COUNT(具体列)才忽略该列NULL。', type:'judge'}
  ]
});
